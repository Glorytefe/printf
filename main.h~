#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>

/**
 * struct printer - Struct printer
 * @format: format to print
 * @f: function to print
 */

typedef struct printer
{
	char *format;
	int (*f)(va_list);
} printer;

#endif
